# Floor 2 Boss Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the shared Floor 2 boss as a phased capstone with lane-specific preparation advantages.

**Architecture:** Keep the encounter data-driven in `data/scenes.json` and `data/items.json`. Add a small runtime/terminal combat hook that applies optional boss advantages based on items already in the player's inventory.

**Tech Stack:** Node.js CommonJS, JSON scene/item data, `test_full.js` validation.

---

### Task 1: Add Red Tests

**Files:**
- Modify: `test_full.js`

- [x] Add tests for boss phases, intro/victory text, loot, lane marker items, and runtime combat modifiers.
- [x] Run `node test_full.js` and confirm the new tests fail against the placeholder boss.

### Task 2: Add Data

**Files:**
- Modify: `data/items.json`
- Modify: `data/scenes.json`

- [x] Add `f2_tactical_readout`, `f2_pattern_key`, and `convergence_core` item definitions.
- [x] Add tactical readout reward to the red optional cache.
- [x] Add pattern key reward to the black puzzle route.
- [x] Replace `floor2_shared_boss` with a three-protocol phased encounter and defined advantage metadata.

### Task 3: Add Combat Advantage Hook

**Files:**
- Modify: `engine/combat.js`
- Modify: `engine/runtime.js`

- [x] Apply boss advantage metadata before combat state is created.
- [x] Support HP, attack, defense, and ability-removal adjustments.
- [x] Keep behavior inert for regular fights and players without marker items.

### Task 4: Verify

**Files:**
- Test: `test_full.js`

- [x] Run `node test_full.js`.
- [x] Confirm all tests pass with zero failures.
