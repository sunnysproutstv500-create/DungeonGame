# Floor 2 Path 1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the red-door Floor 2 lane into a ten-room exploratory path with optional side rooms before the shared Floor 2 boss.

**Architecture:** Use existing `data/scenes.json` scene definitions and `test_full.js` validation. Keep Path 1 as JSON content, with no new engine mechanics unless tests prove a gap.

**Tech Stack:** Node.js CommonJS, JSON scene data, `test_full.js`.

---

## File Structure

- Modify `data/scenes.json`: replace `floor2_red_door` stub and add nine Path 1 rooms plus `floor2_shared_boss`.
- Modify `test_full.js`: add tests for Path 1 room count, optional side rooms, one-way safe-room rule, and shared boss convergence.

### Task 1: Path 1 Structure Tests

- [ ] Add tests in `test_full.js` near existing Floor 2 safe-room tests.
- [ ] Assert Path 1 has exactly ten listed rooms before `floor2_shared_boss`.
- [ ] Assert optional rooms are reachable but not required by the shortest main route.
- [ ] Assert none of the Path 1 rooms link to `floor2_safe_room`.
- [ ] Assert red, blue, and black lanes all route toward `floor2_shared_boss`.
- [ ] Run `node test_full.js` and confirm failures are due to missing Path 1 rooms/routes.

### Task 2: Path 1 Scene Data

- [ ] Replace `floor2_red_door` with a room that points to `floor2_pressure_gallery`.
- [ ] Add `floor2_pressure_gallery`, `floor2_broken_checkpoint`, `floor2_observation_nest`, `floor2_supply_fault`, `floor2_service_crawl`, `floor2_rival_cache`, `floor2_split_conduit`, `floor2_gatehouse`, and `floor2_boss_antechamber`.
- [ ] Add `floor2_shared_boss` as a shared endpoint boss stub with combat data.
- [ ] Update blue and black stubs so their current placeholder exits route toward `floor2_shared_boss`.
- [ ] Run `node test_full.js` and fix scene graph/map issues.

### Task 3: Final Verification

- [ ] Run `node test_full.js`.
- [ ] Confirm zero failures.
- [ ] Summarize changed files and note that this workspace is not a git repo.
